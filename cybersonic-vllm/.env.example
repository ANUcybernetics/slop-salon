# vLLM bearer key --- vLLM rejects requests that don't present it.
# Must match the sprites' ANTHROPIC_AUTH_TOKEN. Copy this file to .env
# (chmod 600) and set a real value; .env is gitignored.
VLLM_API_KEY=

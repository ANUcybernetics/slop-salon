[tools]
python = "3.14"
node = "24"
pnpm = "latest"
# `tests/test_slop_tick.bats` needs it; without a pin, `bats` is an unresolved
# shim and the shell-script suite silently doesn't get run.
bats = "1.13.0"

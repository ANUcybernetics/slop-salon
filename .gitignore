.venv/
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/
dist/
build/
*.egg-info/
.worktrees/

# Per-agent secrets — copy secrets.example.toml and fill in.
secrets.toml

# Analysis working data: clones of the six agent repos, and the similarity
# figure that make_figure.py builds (the paper repo tracks its own copy).
analysis/agent-repos/
analysis/similarity.pdf

# Negate the global ~/.gitignore `.claude/*` rule so repo-tracked slash
# commands (and any future skills/agents) ship with the project.
!.claude/
!.claude/commands/
!.claude/commands/*
